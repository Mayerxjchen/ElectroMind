# virtualenv
.venv/

# Python
__pycache__/
*.py[cod]
*$py.class
.Python
*.egg
*.egg-info/
.eggs/

# build / packaging
build/
dist/
node_modules/
# VitePress (build locally; deploy via .github/workflows/docs.yml → gh-pages)
docs/node_modules/
docs/.vitepress/.temp/
docs/.vitepress/cache/
docs/.vitepress/dist/
site/
pip-wheel-metadata/
*.whl

# test / coverage
.pytest_cache/
.coverage
coverage.xml
htmlcov/
.tox/
.nox/

# type check / linters
.mypy_cache/
.ruff_cache/

# env / secrets
.env
.env.*
!.env.example
# 项目根遗留 pagent.toml / electromind.toml 常含 api_key，勿提交
#（内置默认配置在 src/electromind/resources/default-config.toml）
/pagent.toml
/electromind.toml

# OS / editor
.DS_Store
.idea/
.vscode/

debug

# example runtime data
.pagent/
.electromind/
# 验收证据（A+ v1.0）必须被跟踪；仅忽略其下的临时产物
artifacts/*
!artifacts/skill-migration/
!artifacts/skill-migration/*.json
# 执行内核验收证据（M0/M1-M7）随仓库跟踪，指向实际 tested_commit
!artifacts/acceptance/
!artifacts/acceptance/**
benchmarks/
.trae

# web dev state
editors/web/tsconfig.tsbuildinfo
.superpowers/
# 临时 agent worktrees（嵌套 git 仓库，勿跟踪）
.claude/worktrees/

# coverage 产物（验收脚本生成）
coverage.json
