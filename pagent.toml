# 项目级配置，覆盖 bundled 默认值（src/app/pagent.toml）
# 开发时用 `uv run pagent`；发布后用 `uvx pagent`

max_turns = 12

[provider]
model = "deepseek-v4-flash"
api_key = ""

[sandbox]
backend = "ssh"

[ssh]
host = "machine_root"
config_path = "~/.ssh/config"
workdir = "~/agent"

[skills]
roots = []
